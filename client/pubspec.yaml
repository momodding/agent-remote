name: agentic_remote
description: agenticRemote Flutter client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  shadcn_ui: ^0.55.0
  xterm: ^4.0.0
  web_socket_channel: ^3.0.3
  http: ^1.5.0
  file_picker: ^10.3.2
  image_picker: ^1.2.0
  mobile_scanner: ^7.1.3
  crypto: ^3.0.7
  cryptography: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
